# Jenkins Deployment

This area is reserved for the pinned Jenkins controller, agents, adapter
descriptors, Configuration as Code, Job DSL, reviewed defaults, and operational
interfaces. The reviewed composition Fixture descriptor is present for the local
Stage-adapter seam. The controller, agents, and supported Jenkins installation
arrive in the Jenkins deployment tickets.
