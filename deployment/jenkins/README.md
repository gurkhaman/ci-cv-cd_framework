# Jenkins Deployment

This area is reserved for the pinned Jenkins controller, agents, adapter
descriptors, Configuration as Code, Job DSL, reviewed defaults, and operational
interfaces. Those artifacts are introduced by the Jenkins deployment tickets;
there is no supported Jenkins installation in the foundation slice.
