"""SDI pipeline integration package.

Only the CLI, committed file contracts, and Stage-adapter process interface are
supported interfaces. Python modules remain internal so Stage implementations
can evolve independently.
"""
